# Copy this file to `.env` for local development or `.env.production` for deployments.
# The frontend uses REACT_APP_* variables at build time.
# Base URL of the backend API. Use the relative path when frontend and backend are served from the same origin.
REACT_APP_API_BASE_URL=/api
# Uncomment the next line to point to an absolute backend URL instead.
# REACT_APP_API_BASE_URL=https://example.com/api
# Enable verbose API logging in the browser console (true/false).
REACT_APP_DEBUG=false
